
const preloader = null;
const boot = null;
const mainMenu = null;
const game = null;

const module = {
    preloader,
    boot,
    mainMenu,
    game
}

window.gamedata.modules.data = module;

export default module;